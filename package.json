{
  "name": "adaptive-card",
  "version": "0.0.1",
  "description": "generate validated adaptive card and send it to webhook",
  "type": "module",
  "main": "lib/src/index.js",
  "bin": {
    "adaptive-card": "bin/adaptive-card"
  },
  "scripts": {
    "build": "tsc && cp src/adaptive-card-schema.json lib/src/",
    "pretest": "npm run build",
    "test": "node --test 'lib/test/**/*.test.js'",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "adaptive",
    "card",
    "webhook"
  ],
  "author": "sylvainrivierre@gmail.com",
  "license": "MIT",
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^6.0.2"
  }
}